"""
[Phase 2 예약] Edge-aware 필터, Blooming(하이라이트 광훈) 등 공간(spatial)
연산 - 아직 미구현. V0.1은 픽셀별(pointwise) 연산인 tone_core/color_core
까지만 다루고, 주변 픽셀을 참조하는 공간 연산은 여기에 들어갈 자리만
잡아둔다.
"""


def apply_spatial(img_rgb, **kwargs):
    """Phase 2 스텁 - 아직 아무 연산도 하지 않고 입력을 그대로 반환한다."""
    return img_rgb
